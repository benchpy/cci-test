machine:
  python:
    version: 2.7.10

dependencies:
  override:
    - pip install -U pip
    - pip install -U setuptools
    - make install-test

test:
  pre:
    - make lint
  override:
    - make test
  post:
    - coverage html -d $CIRCLE_ARTIFACTS
